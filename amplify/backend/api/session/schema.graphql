type Session @model @key(fields: ["id", "createdAt", "joined"]) {
    id: ID!
    createdAt: AWSDateTime!
    joined: Int!
    host: User!
    guest: User
    event: [String]
}

type User {
    id: ID!
    name: String
}
